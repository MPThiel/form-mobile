/**
 * Adaptive coach system prompt builder.
 *
 * Phase 0: stubbed. Real implementation in Phase 5.
 *
 * Per CLAUDE.md: system prompts are rebuilt on every API call from live state
 * (profile, today's log, current week plan, streak, mood, energy). Never cache
 * between messages. Tone shifts between strict / balanced / gentle based on the
 * adaptive rules in FORM-mobile-build-spec.md section 12 (ported from v1).
 */
